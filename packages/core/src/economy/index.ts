/**
 * Economy module. The legacy decentralized-compute self-funding manager was
 * removed (Nebula uses an API-key LLM). Reserved for Casper-native economy
 * primitives (treasury limits, spend caps, etc.).
 */
export {}
