{
  "name": "nebula-ai-gateway",
  "version": "0.6.0",
  "type": "module",
  "description": "Nebula gateway daemon: the always-on local brain runtime + listeners (Telegram, approvals) for the nebula agent",
  "license": "MIT",
  "homepage": "https://github.com/rstfulzz/nebula",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rstfulzz/nebula.git",
    "directory": "packages/gateway"
  },
  "bugs": {
    "url": "https://github.com/rstfulzz/nebula/issues"
  },
  "keywords": ["nebula", "ai", "agent", "gateway", "daemon", "casper"],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "bun": ">=1.1"
  },
  "files": ["src", "!src/**/*.test.ts", "bin", "README.md"],
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "bin": {
    "nebula-gateway": "bin/nebula-gateway",
    "nebula-gateway-local": "bin/nebula-gateway-local"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "bun test"
  },
  "dependencies": {
    "@noble/hashes": "^2.2.0",
    "casper-js-sdk": "5.0.12",
    "nebula-ai-core": "0.6.0",
    "nebula-ai-plugin-onchain": "0.6.0",
    "nebula-ai-plugin-system": "0.6.0",
    "nebula-ai-plugin-telegram": "0.6.0",
    "zod": "^3.24.1"
  }
}
