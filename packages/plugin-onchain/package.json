{
  "name": "nebula-ai-plugin-onchain",
  "version": "0.4.0",
  "type": "module",
  "description": "Casper-native on-chain tools for Nebula: native CSPR transfer, staking (earn), validators, balances, and the deterministic fund-control policy engine — every write policy-gated and verified on-chain.",
  "license": "MIT",
  "keywords": ["nebula", "ai", "agent", "casper", "cspr", "staking", "policy", "plugin"],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "bun": ">=1.1"
  },
  "files": ["src", "!src/**/*.test.ts", "README.md"],
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "bun test"
  },
  "dependencies": {
    "casper-js-sdk": "5.0.12",
    "nebula-ai-core": "0.4.0",
    "zod": "^3.23.8"
  }
}
